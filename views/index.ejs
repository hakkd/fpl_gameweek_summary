<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>
<body>
    <div class="header">
        <h1>FPL Gameweek Summary</h1>
    </div>
    <div class="container">
        <form action="/" method="post">
            <select name="gw" class="form-select">
                <option selected>Gameweek</option>
                <!-- for each gameweek data make a list item -->
                <% locals.data.forEach(gw => { %>
                    <% if (gw.finished != false) { %>
                        <option value="<%=gw.id %>"><%=gw.id %></option>
                    <% } %>
                <% }) %>
            </select>
            <button id="btn">Go</button>
        </form>
        <% if (locals.selected) { %>

            <% } else { %>
                <!-- default to gameweek1 -->
                <div class="card">
                    <div class="card-header">
                      <%= locals.data[0].name %>
                    </div>
                    <div class="card-body">
                        <ul class="list-group">
                        <!-- insert gameweek data here -->
                            <li class="list-group-item">Average Score: <%=locals.data[0].average_entry_score %></li>
                            <li class="list-group-item">Highest Scoring Team ID: <%=locals.data[0].highest_scoring_entry %></li>
                            <li class="list-group-item">Highest Score: <%=locals.data[0].highest_score %></li>
                            <li class="list-group-item">Most Selected: <%=locals.data[0].most_selected %></li>
                            <li class="list-group-item">Most Transferred In: <%=locals.data[0].most_transferred_in %></li>
                            <li class="list-group-item">Most Captained: <%=locals.data[0].most_captained %></li>
                            <li class="list-group-item">Most Vice-Captained: <%=locals.data[0].most_vice_captained %></li>
                        </ul>
                    </div>
                  </div>
            <% } %>
    </div>
</body>
</html>